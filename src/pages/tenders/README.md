Tenders page module mirrors `src/pages/home` structure.

Folders:

- components
- data
- hooks
- services
- store (optional)
- types
- index.js

Use `services/tendersService.js` for data fetching; `hooks/useTendersData.js` for view logic.
